<div class="markdownContainer">
    <div class="markdownHeader">
        <h1><%= blog.title %></h1>
    </div>
    <div class="markdownIcons">
        <i onclick="bookmarkPost('<%= blog._id %>')"class="<%= bookmarked === true ? 'fa-solid fa-bookmark' : 'fa-regular fa-bookmark' %>"></i>
        <i onclick="updateLike('<%= blog._id %>')"class="<%= liked === true ? 'fa-solid fa-heart' : 'fa-regular fa-heart'  %>"> <%= totalLikes %></i>
        <i class="fa-regular fa-share-from-square"></i>
    </div>
    <div class="markdownAuthor">
        <h2>Written by <a href="/profile/myprofile/<%= blog.author._id %>"><%= blog.author.userName %></a><span>&bull;</span><%= moment(blog.createdAt).format('MMMM Do') %></h2>
    </div>
    <section class="blogContent">
        <%- blog.sanitizedHtml %>
    </section>
</div>